import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 5,
    padding: 5,
    borderWidth: 1,
    borderColor: '#ced4da',
    width: '100%',
  },
});
